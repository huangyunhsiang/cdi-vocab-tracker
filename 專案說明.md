---
建立AI: Claude Code
建立日期: 2026-07-05
啟動指令: 啟動.bat
---

# 專案說明

## 專案目的
嬰幼兒詞彙發展追蹤 PWA（CDI 公開類別架構）：三軌記錄（理解／表達／手勢）、成長曲線＋詞彙爆發偵測、類別分佈、研究級 CSV 匯出。給使用者與恬恬老師共同記錄自家孩子（2025-07-21 生，現處 Words & Gestures 階段）。Phase 2 預留 LINE 快速記錄入口。

## 技術棧 / 啟動方式
- vanilla JS PWA（無 build step）＋ Chart.js CDN；紙感極簡主題
- 資料層可插拔：預設 localStorage；`js\firebase-config.js`（gitignore，樣板為 `firebase-config.sample.js`）填入設定後啟用 Firestore 同步
- 本機啟動：`python -m http.server 8770`
- 測試：`node --test tests\`（analytics 純函式）

## 版權紅線
MCDI-T 正式題本有版權，**任何情況都不得把題本詞項寫進 repo**；題本走使用者本機檔案載入（`js\wordlist-loader.js`）。

## 交接區（給下一個接手的 AI）
- **目前進度**（2026-07-06）：Phase 1 完成並通過隔離驗收 10/10（node --test 17/17、靜態資源 smoke、版權紅線、獨立腳本驗爆發偵測與 CSV BOM 皆過）。已發布：GitHub `huangyunhsiang/cdi-vocab-tracker`（public）＋ GitHub Pages **https://huangyunhsiang.github.io/cdi-vocab-tracker/**（User 授權，線上 E2E 實測新增詞→localStorage 寫入成功後已清除測試資料）。本機另有一鍵 `啟動.bat`（port 8770）。
- **如何接手**：先讀本檔與 README；資料層唯一入口 `js\store.js`（localStorage 預設，`js\firebase-config.js` 存在時自動切 Firestore）；分析純函式在 `js\analytics.js`（node 可直接測）。
- **Firebase 同步已設定**（2026-07-06）：專案 `cdi-vocab-tracker-huang`（asia-east1 台灣）、Firestore 正式版、Google 登入已啟用、授權網域含 `huangyunhsiang.github.io`、白名單規則已部署（`smalltain@gmail.com`＝恬恬／媽媽、`bear2031@gmail.com`＝雲祥／爸爸，2026-07-06 確認）。`js\firebase-config.js` 已入版控（web 設定客戶端公開，安全靠規則白名單）。App 邏輯：未登入走 localStorage，設定頁按「使用 Google 登入」後切 Firestore，首次登入自動遷移本機資料。**最後一步「實際用 Google 帳號登入」需 User 本人做**（帳號認證不由 AI 代做）。恬恬老師真正的 Gmail 若非 bear2031，需補進 `firebase\firestore.rules` 白名單後 `firebase deploy --only firestore:rules`。
- **待辦 / 下一步**：①雲祥（bear2031）與恬恬（smalltain）各自在 App 設定頁完成一次 Google 登入，驗證雙裝置同步（白名單兩人皆已就緒）；②Phase 2：LINE 快速記錄入口（需常駐後端，參考 060/062）。
- **私有題本**（2026-07-06，僅本機）：`私有題本\` 內有台灣華語 CDI 題項兩份（WG 367 詞＝現齡用、WS 696 詞＝約 16 月起），來源 Wordbank 開放資料庫（見資料夾內 `來源說明.md`），已用 App 解析器驗證可載入。**此資料夾在 .gitignore，任何情況不得移入版控或公開**；類別表已為此補齊 5 個 CDI 公開類別（furniture_room／outside／place／quantifier／connecting）。
- **已知問題 / 坑**：①CSV 匯出必須帶 UTF-8 BOM（Excel 繁中），已有測試釘住；②爆發偵測定義在 js\analytics.js JSDoc（50 詞後、近 7 天新增 ≥10 或近 14 天 ≥20）；③Node v24 跑 `node --test tests\`（帶目錄參數）會 MODULE_NOT_FOUND，要用無參數 `node --test` 或指明檔案；④`js\categories.js` 的 DEMO_WORDS 目前是死碼（保留供 UI 示意，未注入正式資料路徑）；⑤版權紅線：任何情況都不得把 MCDI-T 題本詞項寫進 repo；⑥**PWA service worker 快取**：已改 network-first（v2）；但更新後若使用者仍看到舊版（尤其之前開過舊版的裝置），請關分頁重開或到瀏覽器「網站設定→清除資料」；⑦GitHub Pages 純靜態站靠根目錄 `.nojekyll` 才會原樣部署所有檔（否則 Jekyll 會吃掉部分檔）。
