---
建立AI: Claude Code
建立日期: 2026-07-05
啟動指令: 啟動.bat
---

# 專案說明

## 專案目的
嬰幼兒詞彙發展追蹤 PWA（CDI 公開類別架構）：三軌記錄（理解／表達／手勢）、成長曲線＋詞彙爆發偵測、類別分佈、研究級 CSV 匯出。給使用者與恬恬老師共同記錄自家孩子（2025-07-21 生，現處 Words & Gestures 階段）。Phase 2 預留 LINE 快速記錄入口。

## 技術棧 / 啟動方式
- vanilla JS PWA（無 build step）＋ Chart.js CDN；紙感極簡主題
- 資料層可插拔：預設 localStorage；`js\firebase-config.js`（gitignore，樣板為 `firebase-config.sample.js`）填入設定後啟用 Firestore 同步
- 本機啟動：`python -m http.server 8770`
- 測試：`node --test tests\`（analytics 純函式）

## 版權紅線
MCDI-T 正式題本有版權，**任何情況都不得把題本詞項寫進 repo**；題本走使用者本機檔案載入（`js\wordlist-loader.js`）。

## 交接區（給下一個接手的 AI）
- **目前進度**（2026-07-05）：Phase 1 完成並通過隔離驗收 10/10（node --test 17/17、靜態資源 smoke、版權紅線、獨立腳本驗爆發偵測與 CSV BOM 皆過）。本機 git 已 commit。
- **如何接手**：先讀本檔與 README；資料層唯一入口 `js\store.js`（localStorage 預設，`js\firebase-config.js` 存在時自動切 Firestore）；分析純函式在 `js\analytics.js`（node 可直接測）。
- **待辦 / 下一步**：①User 照 `docs\FIREBASE-SETUP.md` 建 Firebase 專案＋開 Google 登入，把恬恬老師 email 填進 `firebase\firestore.rules` 的 `WIFE_EMAIL_HERE` 佔位；②部署（GitHub Pages 或其他）屬對外發布，需 User 裁決；③Phase 2：LINE 快速記錄入口（需常駐後端，規劃時參考 060/062 經驗）。
- **已知問題 / 坑**：①CSV 匯出必須帶 UTF-8 BOM（Excel 繁中），已有測試釘住；②爆發偵測定義在 js\analytics.js JSDoc（50 詞後、近 7 天新增 ≥10 或近 14 天 ≥20）；③Node v24 跑 `node --test tests\`（帶目錄參數）會 MODULE_NOT_FOUND，要用無參數 `node --test` 或指明檔案；④`js\categories.js` 的 DEMO_WORDS 目前是死碼（保留供 UI 示意，未注入正式資料路徑）；⑤版權紅線：任何情況都不得把 MCDI-T 題本詞項寫進 repo。
